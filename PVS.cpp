#include "Position.h"

int pvSearch(int alpha, int beta, int depth, Position position) {
    if (depth == 0) {
        return evaluate(alpha, beta);
    }
    bool bSearchPv = true;

    if (position.isWhiteTurn)
    for ()
}